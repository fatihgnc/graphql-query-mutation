type Query {
  users: [User!]
  jobs: [Job!]
  job(id: String!): Job
  user(id: String!): User
  company(id: String!): Company
  companiesByName(name: String!): [Company!]!
  jobsByTitle(name: String!): [Job!]!
  usersByFullname(name: String!): [User!]!
}

type User {
  id: String!
  fullname: String!
  job: Job
  company: Company
}

type Job {
  id: String!
  title: String!
  description: String!
}

type Company {
  id: String!
  name: String!
  description: String!
  availableJobs: [Job!]
}
